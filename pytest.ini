[pytest]
# Coloca a raiz do repositório no sys.path para que os testes possam
# importar o módulo top-level `agente_ia_control_desk` independentemente
# de como o pytest é invocado (console script `pytest` ou `python -m pytest`).
pythonpath = .
testpaths = tests
