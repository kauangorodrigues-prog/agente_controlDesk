# ── Núcleo (obrigatório em qualquer modo) ────────────────────────────────
numpy
pandas
requests
joblib
python-dotenv
scikit-learn
SQLAlchemy>=2.0
psycopg2-binary
openpyxl
APScheduler

# ── Modo "api" (main.py api) ──────────────────────────────────────────────
fastapi
uvicorn[standard]
python-jose[cryptography]
passlib[bcrypt]
python-multipart
prometheus-client
pydantic

# ── Modo "dashboard" (streamlit run dashboard_app.py) ─────────────────────
streamlit
plotly

# ── Analisador de ligações ALO / NÃO ALO ─────────────────────────────────
# Opcional: sem o pacote (ou sem ANTHROPIC_API_KEY) o analisador cai
# automaticamente na heurística offline, sem gerar erro.
anthropic

# ── Opcionais ──────────────────────────────────────────────────────────────
holidays          # sincronização automática de feriados nacionais

# Sem este pacote instalado, ForecastService.generate_forecast() cai
# automaticamente no fallback de média móvel, sem gerar erro.
# prophet
