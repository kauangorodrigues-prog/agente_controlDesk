# Alvo do backend usado pelo proxy do Vite em desenvolvimento.
VITE_API_TARGET=http://localhost:8000

# Produção: origem do backend (deixe vazio em dev — o proxy do Vite trata /api).
# ex.: https://controldesk-cobranca-api.vercel.app
VITE_API_URL=
